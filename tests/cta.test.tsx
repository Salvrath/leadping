import React from"react";import{render,screen,fireEvent}from"@testing-library/react";import{describe,it,expect,vi}from"vitest";import{TrackedLink}from"@/components/tracked-link";vi.mock("@/lib/analytics",()=>({track:vi.fn()}));describe("central CTA",()=>{it("renders navigable anchor",()=>{render(React.createElement(TrackedLink,{href:"#demo",event:"hero_primary_cta_clicked"},"Testa demonstrationen"));const link=screen.getByRole("link");expect(link).toHaveAttribute("href","#demo");fireEvent.click(link)})});
