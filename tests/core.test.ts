import{describe,it,expect}from"vitest";import{calculateMonthlyValue}from"@/lib/calculator";import{leadSchema}from"@/lib/lead-schema";import{filterAnalyticsProperties}from"@/lib/analytics";import{nextDemoStep}from"@/lib/demo";import{getCheckout}from"@/lib/checkout";import{allows,defaultConsent}from"@/lib/consent";
describe("Textback core",()=>{it("calculates conservative monthly value",()=>expect(calculateMonthlyValue({missed:15,relevant:50,margin:2000,recovered:15})).toBe(9743));it("validates a complete pilot lead",()=>expect(leadSchema.safeParse({company:"Testverkstad",contact:"Anna",email:"anna@example.se",phone:"0701234567",workshopPhone:"081234567",telephony:"Telia",missedCalls:10,employees:3,privacy:true,authority:true}).success).toBe(true));it("rejects missing consent",()=>expect(leadSchema.safeParse({}).success).toBe(false));it("filters personal data from analytics",()=>expect(filterAnalyticsProperties({email:"a@b.se",phone:"123",field:"margin",value:2})).toEqual({field:"margin",value:2}));it("moves through demo states",()=>{expect(nextDemoStep("incoming")).toBe("sending");expect(nextDemoStep("replied")).toBe("lead");expect(nextDemoStep("lead")).toBe("incoming")});it("requires secure checkout URL and falls back",()=>{expect(getCheckout("https://pay.example.se").ready).toBe(true);expect(getCheckout(undefined)).toEqual({href:"#checkout-unavailable",ready:false})});it("gates optional categories on consent",()=>{expect(allows(defaultConsent,"necessary")).toBe(true);expect(allows(defaultConsent,"analytics")).toBe(false)})});
